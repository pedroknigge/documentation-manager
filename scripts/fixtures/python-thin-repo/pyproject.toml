[project]
name = "hello-app"
version = "0.0.1"
description = "Hardening fixture — thin Python repo (no docs hub)"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
