# monorepo-thin (fixture)

Synthetic **thin monorepo** for Documentation Manager hardening (Slice B monorepo hubs).

- Has: root `package.json` workspaces + `pnpm-workspace.yaml`, `packages/api`, `packages/web`
- Does **not** have: root `AGENTS.md`, root `docs/`, package hubs

Not a product app — package-index / detect-packages smoke only.
